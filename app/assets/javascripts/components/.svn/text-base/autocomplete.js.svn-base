$(document).click(function(event) 
{ 
    /*
if(!$(event.target).closest('.auto_complete').length) {
		
		var return_url = $("#ddl_return_url").val();
		
		
		if($('.auto_complete').is(":visible")) 
		{
          jQuery.get(return_url)
        }
		else
		{
			if( $(event.target).hasClass('remove-ddl-selected')) 
			{
				  $(".selected_ddlb_item").val("");
				  var html_dummy_data = dummy_list();
  				  $(".list-form").html(html_dummy_data)
				  
				  $(".filter_form").submit();
			}
			
			
			
		}
    }  
*/      
});




/*
$(document).on('click', '.drop_down_item', function(e)
{ 
  var name = $(this).find(".name").text();
  var customer_id =  $(this).find(".name").attr("data-customer-id");
  $(".selected_ddlb_item").val(customer_id);
  var n = "<div class='input-select-field'>"+name+" <span class='fa fa-times remove-ddl-selected select-icon'></span></div>"
  
  setTimeout(selectedDropDownItem, 100, selectedString);
	
});

function selectedDropDownItem(selectedString)
{
	$("#customer_drop_down").html(selectedString)
	 
}
*/

